import React from "react";
import styled from "styled-components";

const Modal = styled.div`
  background: #f3f1ff;
`;

const Home: React.SFC = () => <Modal>I am a modal</Modal>;

export default Home;
